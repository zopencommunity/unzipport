diff --git a/extract.c b/extract.c
index 1acd769..9884f1b 100644
--- a/extract.c
+++ b/extract.c
@@ -749,7 +749,6 @@ int extract_or_test_files(__G)    /* return PK-type error code */
     send the summary to stdout so that people doing the testing in the back-
     ground and redirecting to a file can just do a "tail" on the output file.
   ---------------------------------------------------------------------------*/
-
 #ifndef SFX
     if (no_endsig_found) {                      /* just to make sure */
         Info(slide, 0x401, ((char *)slide, LoadFarString(EndSigMsg)));
@@ -1658,6 +1657,9 @@ static int extract_or_test_member(__G)    /* return PK-type error code */
             G.outfile = freopen("", "wb", stdout);   /* VAC++ ignores setmode */
 #else
             G.outfile = stdout;
+	    #ifdef __MVS__ /* z/OS: handle ccsid conversion for terminal */
+            zos_open_stdout(__G);
+#endif /* __MVS__ */
 #endif
 #ifdef DOS_FLX_NLM_OS2_W32
 #if (defined(__HIGHC__) && !defined(FLEXOS))
@@ -1727,7 +1729,7 @@ static int extract_or_test_member(__G)    /* return PK-type error code */
             while ((b = NEXTBYTE) != EOF) {
                 *G.outptr++ = (uch)b;
                 if (++G.outcnt == wsize) {
-                    error = flush(__G__ redirSlide, G.outcnt, 0);
+		     error = flush(__G__ redirSlide, G.outcnt, 0);
                     G.outptr = redirSlide;
                     G.outcnt = 0L;
                     if (error != PK_COOL || G.disk_full) break;
